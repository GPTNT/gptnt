# @package _global_

defaults:
  - _self_
  - pairing: with_self
  - communication_style: sequential
  - condition: multiple_modules_2
  - players: default

mission_generator:
  config:
    time_limit: null
