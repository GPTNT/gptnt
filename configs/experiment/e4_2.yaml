# @package _global_

defaults:
  - _self_
  - pairing: with_best
  - communication_style: sequential
  - condition: repeated_modules_5
  - players: default
# pairing_generator:
# best_model: gpt4o

mission_generator:
  config:
    time_limit: null
