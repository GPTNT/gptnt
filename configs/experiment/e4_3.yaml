# @package _global_

defaults:
  - _self_
  - pairing: with_best_defuser
  - communication_style: sequential
  - condition: repeated_modules_2
  - players: default
# pairing_generator:
# best_model: gpt4o

mission_generator:
  config:
    time_limit: null
