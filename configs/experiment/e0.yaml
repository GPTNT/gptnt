# @package _global_

defaults:
  - override /mission_condition: single_module
  - override /communication_style: sync
  - override /pairing: with_best_defuser
  - override /players: test
  - override /think_framework: act
  - override /player_specs@defuser_protocol: defuser
  - override /player_specs@expert_protocol: expert
  - _self_

pairing_generator:
  pairing_type: with_best_defuser
  best_model: test-defuser

mission_generator:
  config:
    time_limit: 60
