# @package _global_

defaults:
  - override /experiment/mission_condition: single_module
  - override /experiment/communication_style: sync
  - override /experiment/pairing: no_expert
  - override /experiment/player_specs@defuser_protocol: solo_random
  - override /experiment/player_specs@expert_protocol: none

  - _self_

players:
  all:
    - test-random

experiment_generator:
  _target_: gptnt.experiments.experiments.ExperimentGenerator
  attempts_per_mission: 100
