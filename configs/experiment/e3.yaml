# @package _global_

defaults:
  - _self_
  - pairing: no_partner
  - communication_style: sequential
  - condition: single_module
  - players: default

mission_generator:
  config:
    time_limit: null
