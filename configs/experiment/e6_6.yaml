# @package _global_

defaults:
  - _self_
  - pairing: with_best_defuser
  - communication_style: parallel
  - condition: multiple_modules_n
  - players: default

mission_generator:
  config:
    time_limit: 300
    mission_specs_dir: configs/experiment/mission_specs/multiple_module_n
