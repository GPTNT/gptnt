# @package _global_

defaults:
  - _self_
  - pairing: with_best_expert
  - communication_style: parallel
  - condition: multiple_modules_n
  - players: default

mission_generator:
  config:
    time_limit: 300
