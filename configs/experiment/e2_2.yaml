# @package _global_

defaults:
  - _self_
  - pairing: with_best_expert
  - communication_style: sequential
  - condition: single_module
  - players: smaller
# pairing_generator:
# best_model: gpt4o

mission_generator:
  config:
    time_limit: null
