# @package _global_

defaults:
  - _self_
  - pairing: with_self
  - communication_style: parallel
  - condition: multiple_modules_2_front
  - players: default

mission_generator:
  config:
    time_limit: 150
