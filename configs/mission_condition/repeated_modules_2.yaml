# @package _global_

experiment_generator:
  condition: repeated_modules_2

mission_generator:
  num_seeds_per_mission: 3
  config:
    _target_: gptnt.experiments.missions.MissionGeneratorConfig
    time_limit: ???
    allow_back_placement: false

    n_modules_min: 2
    n_modules_max: 2
    sample_from_modules: false
    allow_repeat_module: true

    min_optional_widgets: 1
    max_optional_widgets: 5
