# @package _global_

defaults:
  - _self_
  - hydra: default

  - mission_condition: single_module

  - communication_style: sync

  - players: default
  - pairing: no_expert

  - think_framework: react

  - player_specs@defuser_player_spec: solo_defuser
  - player_specs@expert_player_spec: none

  - experiment: null

  - override hydra/job_logging: none
  - override hydra/hydra_logging: none

task_name: generate_experiments

defuser_player_spec: ???
expert_player_spec: ???
communication_style: ???
framework:
  thinking_framework: ???
  allow_thoughts_output: ???
  allow_thoughts_in_history: ???
  allow_outputs_in_history: ???

mission_generator:
  _target_: gptnt.experiments.missions.MissionGenerator
  seed: 10
  num_seeds_per_mission: ???
  config: ???

experiment_generator:
  _target_: gptnt.experiments.experiments.ExperimentGenerator
  condition: ???
  defuser_player_spec: ${defuser_player_spec}
  expert_player_spec: ${expert_player_spec}

pairing_generator:
  _target_: gptnt.experiments.pairing.PairingGenerator
  pairing_type: "pairwise"
  best_model: null
  all_players: ???
