# @package _global_

defaults:
  - _self_
  - hydra: default

  - mission_condition: single_module

  - communication_style: sync

  - players: default
  - pairing: no_expert

  - player_specs@defuser_protocol: solo_defuser
  - player_specs@expert_protocol: none

  - experiment: null

  - override hydra/job_logging: none
  - override hydra/hydra_logging: none

task_name: generate_experiments

defuser_protocol: ???
expert_protocol: ???
communication_style: ???
framework: ???

mission_generator:
  _target_: gptnt.experiments.missions.MissionGenerator
  seed: 10
  num_seeds_per_mission: ???
  config: ???

experiment_generator:
  _target_: gptnt.experiments.experiments.ExperimentGenerator
  condition: ???
  defuser_protocol: ${defuser_protocol}
  expert_protocol: ${expert_protocol}

pairing_generator:
  _target_: gptnt.experiments.pairing.PairingGenerator
  pairing_type: ???
  best_model: null
  all_players: ${all_players}
