# @package player

defaults:
  - vllm
  - _self_

metadata:
  player_name: internvl3-2b

agent:
  output_type:
    variant: "gemini"

  model:
    model_name: OpenGVLab/InternVL3-2B
    provider:
      base_url: http://localhost:11434/v1
