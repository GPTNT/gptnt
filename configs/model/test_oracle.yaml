# @package player

defaults:
  - _self_

capabilities:
  player_name: test-oracle
  supports_structured_output: true

action_predictor:
  agent:
    retries: 0
    model:
      _target_: gptnt.players.dummy.models.MagicDefuserModel
