# @package player

defaults:
  - _self_

action_predictor:
  agent:
    model:
      _target_: pydantic_ai.models.openai.OpenAIModel
      model_name: ???
      provider:
        _target_: pydantic_ai.providers.openai.OpenAIProvider
        base_url: ???
