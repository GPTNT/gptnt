# @package player

defaults:
  - _self_

capabilities:
  player_name: test-defuser
  use_structured_outputs: true
  structured_output_mode: native
  interaction_location_method: set-of-marks

action_predictor:
  agent:
    retries: 0
    model:
      _target_: gptnt.players.dummy.models.DummyDefuserModel
