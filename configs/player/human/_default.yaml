# @package player

defaults:
  - _self_

_target_: gptnt.players.human.controller.Controller

metadata:
  player_type: human

view: ???

gradio_launch_kwargs:
  share: false

tracker:
  _target_: gptnt.players.metrics.PlayerEpisodeTracker
  wandb_init_kwargs: { "project": "gptnt" }
