# @package player

defaults:
  - ai/_default
  - _self_

_target_: gptnt.players.defuser.MDPDefuserPlayer

game_client:
  _target_: gptnt.ktane.client.KtaneClient
  client:
    _target_: httpx.AsyncClient
    base_url: "${ktane_client_host}:${ktane_client_port}"

  set_of_marks_painter:
    _target_: gptnt.som.som.SetOfMarksHandler
    add_label: true
    add_bbox: true
