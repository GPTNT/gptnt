# @package player

defaults:
  - _default
  - _self_

_target_: ???

metadata:
  player_role: defuser

game_client:
  _target_: gptnt.ktane.client.KtaneClient
  client:
    _target_: httpx.AsyncClient
    base_url: "http://localhost:1245"
  image_resizer:
    _target_: gptnt.processors.image_resizer.ImageResizer
    target_width: 512
    target_height: 512

agent:
  output_type:
    _target_: gptnt.players.ai.output_type.get_defuser_output_type
