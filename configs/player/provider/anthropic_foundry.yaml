# @package player.action_predictor.agent.model

provider:
  _target_: pydantic_ai.providers.anthropic.AnthropicProvider
  anthropic_client:
    _target_: anthropic.AsyncAnthropicFoundry
    # api_key: ${env:ANTHROPIC_FOUNDRY_API_KEY}
    # resource: ${env:ANTHROPIC_FOUNDRY_RESOURCE}
    # base_url: ${env:ANTHROPIC_FOUNDRY_BASE_URL}
