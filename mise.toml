[tasks.format]
description = "Format/lint all files in one go"
run = "uv run pre-commit run -a"

[tasks.tests]
description = "Run all tests"
run = "uv run pytest"

[tools]
python = "3.12"
uv = "latest"
