spec_version: 2
suites:
  - single-pairwise-sync
rooms: 2

players:
  - player: test-defuser
  - player: test-expert

# Where the ledger of results is stored:`local` (on-disk outputs, so no W&B) or `wandb`.
# W&B logging must be configured with the various WANDB_* environment variables.
source: local

observability: limited
