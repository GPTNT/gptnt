### Formatting Requirements

#### Answer

- Follow the exact JSON format for all commands as they are parsed by an incredibly strict validator.
- Provide the entire response as a single JSON object.
- Only respond with the JSON object. Never wrap anything in `json` tags or code fences.
- Output one command per turn only.
