### Coordinates

The resolution of the screen is {IMAGE_WIDTH}x{IMAGE_HEIGHT} pixels. Coordinates are measured from the top-left corner: x (pixels from left edge), y (pixels from top edge). To interact with a UI element, identify a (x, y) pixel coordinate that falls within the element. Provide coordinates for a game action in the format `{"x": <int>,"y": <int>}`.
