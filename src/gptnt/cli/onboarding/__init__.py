"""Onboarding commands: generate specs/missions, list configs, and measure image tokens.

Single-file cyclopts commands grouped here so the `cli/` top level carries no loose command
modules. Each is registered by lazy import-path string in `cli/__main__.py`.
"""
