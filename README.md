# GPTNT

## Repository Structure

To reduce the complexity of the repository, we have split the code into multiple packages and use uv's workspaces to keep it all together. If you only want to work on a certain part, you can do.

The main package found in `src/gptnt` is the primary entrypoint for the entire project.
